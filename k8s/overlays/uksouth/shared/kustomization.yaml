apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ticketing

resources:
  - ../../../base/shared
  - configmap.yaml

labels:
  - includeSelectors: false
    pairs:
      deployment.region: uksouth
