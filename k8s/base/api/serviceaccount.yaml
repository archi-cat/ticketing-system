apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-service-account
  namespace: ticketing
  annotations:
    # Client ID gets injected per-region via the overlay
    azure.workload.identity/client-id: PLACEHOLDER
  labels:
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: ticketing-system
